{
  "name": "react-orbitjs",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "watch": "lerna run --parallel watch",
    "release": "lerna publish"
  },
  "devDependencies": {
    "lerna": "^3.13.4"
  }
}
