import React from "react"
import Code from "./../components/Code"

const Introduction = () => (
  <section>
    <h2>Introduction</h2>
    <p>
      This package attempts to make it easier to work with <a
      href="http://orbitjs.com/">Orbit</a> in a <a
      href="https://reactjs.org/">React</a> environment. In a nutshell it's a
      transform listener, updating a component props with records as they are
      changed.
    </p>
    <p>
      If you're already familiar with <a
      href="https://github.com/reactjs/redux/">redux</a> in combination with <a
      href="https://github.com/reactjs/react-redux">react-redux</a>, you already
      know how to use this package.
    </p>
  </section>
)

export default Introduction